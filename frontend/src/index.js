import React from 'react';
import ReactDOM from 'react-dom/client';
import JerseyShop from './jersey-shop-supabase.jsx';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <JerseyShop />
  </React.StrictMode>
);
